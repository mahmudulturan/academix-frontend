import { FC } from 'react';
import { Button } from '@/components/ui/button';

const page: FC = () => {
    return (
        <div>
            <Button>
                <span>Hello</span>
            </Button>
        </div>
    );
};

export default page;